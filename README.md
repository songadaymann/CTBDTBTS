# CTBD TBTS

Static browser game for Cloudflare Pages.

## Deploy

- Framework preset: `None`
- Build command: none
- Output directory: `.`

## Notes

- Game assets used at runtime are stored in this repo under the project root, `sounds/`, and `effects/`.
- Third-party libraries are currently loaded from their public CDNs in `index.html`.
